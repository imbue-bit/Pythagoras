# --- General Application Settings ---
API_PORT=8000

# --- OpenAI API Key ---
# 强烈建议使用 Docker secrets 或其他 secrets 管理工具来处理此密钥
OPENAI_API_KEY="sk-YourOpenAIKeyGoesHere"
LLM_MODEL="gpt-4-turbo"

# --- Database Configuration ---
# 选择 'postgresql' 或 'sqlite'
DATABASE_TYPE="postgresql"

# PostgreSQL Settings (仅当 DATABASE_TYPE="postgresql" 时使用)
DB_HOST=db # Docker Compose 服务名
DB_PORT=5432
DB_USER=pythagoras_user
DB_PASSWORD=a_very_secure_password
DB_NAME=pythagoras_db

# SQLite Settings (仅当 DATABASE_TYPE="sqlite" 时使用)
# 路径相对于容器内的 /app 目录
DATABASE_NAME="data/pythagoras.db"

# --- Redis Cache Configuration ---
# 设为 true 以启用 Redis 缓存，否则将使用内存缓存
USE_REDIS=true
REDIS_HOST=redis # Docker Compose 服务名
REDIS_PORT=6379
REDIS_DB=0
